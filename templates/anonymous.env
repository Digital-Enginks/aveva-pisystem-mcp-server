# ==============================================================================
# AVEVA PI System MCP Server - Environment Template: ANONYMOUS AUTHENTICATION
# ==============================================================================
# Use this file if your PI Web API server is configured to allow anonymous (unauthenticated) access.
# Note: Anonymous mode must only be used in read-only setups and never in production.
#
# INSTRUCTIONS:
# 1. Copy this file to a new file named '.env' in the root directory.
# 2. Fill in the values below.
# 3. Start the server.

# ------------------------------------------------------------------------------
# 1. Server Address & Mode
# ------------------------------------------------------------------------------
# The HTTPS address of your company's PI Web API server.
PIWEBAPI_BASE_URL=https://pi-web-api-server.company.com/piwebapi

# Select "stdio" (recommended for local use in Claude Desktop) or "http" (for network use).
MCP_TRANSPORT=stdio

# ------------------------------------------------------------------------------
# 2. Authentication Configuration
# ------------------------------------------------------------------------------
# We are choosing "anonymous" mode.
PIWEBAPI_AUTH_MODE=anonymous

# We must set this flag to true to explicitly confirm anonymous connection.
PIWEBAPI_ALLOW_ANONYMOUS=true

# ------------------------------------------------------------------------------
# 3. Security & Trust Settings
# ------------------------------------------------------------------------------
# Verify the certificates. Keep this as true.
PIWEBAPI_TLS_VERIFY=true

# If your company uses private certificates, supply the path to the Root CA certificate:
# PIWEBAPI_TLS_CA_FILE=C:\certs\company-root-ca.pem

# ------------------------------------------------------------------------------
# 4. Feature Settings
# ------------------------------------------------------------------------------
# Anonymous mode is ALWAYS read-only: the server refuses to start if
# MCP_READ_ONLY is not true. Keep both values as they are.
MCP_READ_ONLY=true
MCP_WRITE_TOOLS_ENABLED=false

# Log detail level: debug (detailed) | info (normal) | warn (warnings) | error (errors)
MCP_LOG_LEVEL=info
